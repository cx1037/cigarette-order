name: smoke_inventory_app
description: Smoke inventory manager
publish_to: "none"

version: 1.1.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^2.3.0
  path: ^1.8.3
  shared_preferences: ^2.2.2
  file_picker: ^8.0.0+1
  excel: ^2.1.0
  mobile_scanner: ^3.5.7
  url_launcher: ^6.3.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
